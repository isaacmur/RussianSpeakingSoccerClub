[build]
  command = "npx expo export --platform web"
  publish = "dist"

[build.environment]
  # Static rendering runs the Supabase client in Node; @supabase/realtime-js needs a
  # global WebSocket, which only exists in Node 22+. (Node 20 fails the build.)
  NODE_VERSION = "22"

# SPA fallback: Netlify serves existing static files first (index.html, sign-in.html,
# admin/*, etc.), then rewrites any unmatched path — the dynamic [id] routes and
# deep links / refreshes — to the app so expo-router can resolve them client-side.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
